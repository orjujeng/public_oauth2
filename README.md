# public_oauth2
## regist oauth2 api must have client id and rederidect url and the serect key of client must generated by oauth api

##change to the oauth2 login page input with password and username

##return with scode and get token url

##scode + serect visit token url 

## return with token
